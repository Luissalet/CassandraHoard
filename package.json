{
  "name": "cassandra-hoard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Observabilidad del stack de IA local: estado de cada servicio, incidencias con causa probable, logs, GPU y reinicios opcionales, para asistentes con MCP.",
  "scripts": {
    "client": "vite",
    "build": "vite build",
    "dev": "python scripts/dev.py",
    "start": "python -m cassandra_hoard",
    "open": "python scripts/launch.py",
    "test": "python -m pytest -q",
    "mcp": "python mcp_server.py"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "@vitejs/plugin-react": "^4.4.0",
    "tailwindcss": "^4.1.0",
    "vite": "^6.3.0"
  },
  "engines": {
    "node": ">=22.13"
  }
}
